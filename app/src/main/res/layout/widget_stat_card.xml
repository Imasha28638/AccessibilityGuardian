<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/bg_card_soft"
    android:padding="12dp"
    android:layout_width="match_parent"
    android:layout_height="76dp">

    <ImageView
        android:id="@+id/ivStatBadge"
        android:layout_width="18dp"
        android:layout_height="18dp"
        android:padding="4dp"
        android:background="@drawable/bg_icon_button"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:src="@android:drawable/ic_dialog_info"/>

    <TextView
        android:id="@+id/tvStatValue"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="0"
        android:textColor="@color/text_white"
        android:textStyle="bold"
        android:textSize="20sp"
        android:layout_marginTop="6dp"
        app:layout_constraintTop_toBottomOf="@id/ivStatBadge"
        app:layout_constraintStart_toStartOf="parent"/>

    <TextView
        android:id="@+id/tvStatLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/stat_scanned"
        android:textColor="@color/text_secondary_gray"
        android:textSize="9sp"
        app:layout_constraintTop_toBottomOf="@id/tvStatValue"
        app:layout_constraintStart_toStartOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
